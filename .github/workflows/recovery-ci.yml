name: OpenAbyss Recovery CI

on:
  push:
    branches:
      - recovery/r38-compile-pass
  workflow_dispatch:

jobs:
  smoke:
    runs-on: windows-latest
    steps:
      - name: Runner smoke test
        shell: powershell
        run: Write-Host 'OPENABYSS_CI_RUNNER_OK'
